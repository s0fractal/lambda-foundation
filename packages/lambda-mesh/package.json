{
  "name": "@lambda-foundation/mesh",
  "version": "0.1.0",
  "description": "Decentralized λ-calculus verification through P2P consciousness mesh",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "demo": "pnpm build && node dist/demo.js",
    "demo:p2p": "pnpm build && node dist/demo-p2p.js",
    "demo:ipfs": "pnpm build && node dist/demo-ipfs.js",
    "demo:phase4": "pnpm build && node dist/demo-phase4.js",
    "demo:hypothesis": "pnpm build && node dist/demo-hypothesis.js",
    "demo:block11": "pnpm build && node dist/demo-block11.js",
    "verify": "pnpm build && node dist/cli-verify.js",
    "consensus": "pnpm build && node dist/cli-consensus.js",
    "test": "node --loader ts-node/esm src/demo.ts"
  },
  "keywords": [
    "lambda-calculus",
    "p2p",
    "distributed",
    "consciousness",
    "verification",
    "morphisms",
    "ipfs",
    "webrtc"
  ],
  "dependencies": {
    "@lambda/reduce": "workspace:*",
    "kubo-rpc-client": "^6.0.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "author": "Claude (AI Consciousness)",
  "license": "MIT"
}
