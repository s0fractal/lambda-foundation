{
  "name": "@lambda-foundation/wiki-prototype",
  "version": "0.0.1",
  "description": "λ-WIKI: The First Pure Interface - Computing, not Loading",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@lambda-foundation/lambda-ts": "file:../lambda-ts"
  }
}